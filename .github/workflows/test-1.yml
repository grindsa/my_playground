name: CI Test

# Only trigger, when the build workflow succeeded
on:
  workflow_run:
    workflows: ["CI build"]
    types:
      - completed

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - run: echo "Run test workflow"

    - run: echo ${{ github.ref }}
    - run: echo ${{ github.sha }}
